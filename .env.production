# Production Environment Variables (prototype – mock mode)

# API Configuration (not used in mock mode)
VITE_API_BASE_URL=https://api.poski-real.cz/v1
VITE_API_TIMEOUT=30000

# Prototype: use mock data (no backend yet)
VITE_USE_MOCK=true

# Disable debug logging in production
VITE_DEBUG=false
